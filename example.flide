Begin
    DisableCursor

    Label "Cursor disabled."

    Label "An example of Label" Wait 1 New
    Label "An example of Wait & New" Wait 1 New

    Left   "An example of left aligned Label" Wait 1 New
    Center "An example of center aligned Label" Wait 1 New
    Right  "An example of right aligned Label" Wait 1 New

    CenterXY "An example of center (xy) aligned Label"
    EnableCursor
    CenterXY "Cursor enabled."

    Wait 1 New
End